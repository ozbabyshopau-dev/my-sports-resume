# Supabase Auth Phase 1 frontend testing only.
# Never put your Supabase service role key in frontend env files.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ENABLE_BACKEND=false
VITE_APP_ENV=local

# Turn backend on only when you are ready to test real Supabase Auth.
# Sports profile, highlight, opportunity, shortlist, request, and admin data stay local in this phase.
